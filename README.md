Just a basic Docker config used to spin up a basic 'Hello World' for Docker.

https://docs.docker.com/get-started/part2/

Docker image has already been built and pushed. To run the existing image:

`docker run -d -p 4000:80 andrewotoole/get-started:part2`
